#!/bin/env bash

set -o errexit
set -o nounset
set -o pipefail
# set -o xtrace

# download nebula https://github.com/slackhq/nebula/releases/download/v1.4.0/nebula-linux-amd64.tar.gz
#
# untar
# copy config
# render and install systemd unit
